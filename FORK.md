# Fork の変更点

このリポジトリは [wxf4150/esdump](https://github.com/wxf4150/esdump) の fork です。
本家から分岐した後に加えた機能追加とバグ修正をここにまとめます。

分岐点は本家の [`51344b7` (readme update)](https://github.com/wxf4150/esdump/commit/51344b7782bc3be4b47edc37e64850ca816c439c) です。

> [!CAUTION]
> **出力フォーマットが本家と互換ではありません。双方向とも読めません。**
> フィールド名を変更したため、この fork が出力したダンプを本家の esdump に
> 読ませることも、本家が出力したダンプをこの fork に読ませることもできません。
> どちらの向きでも **エラーにならず、データが失われます**。
> 詳細は [出力フォーマットの変更](#出力フォーマットの変更) を参照してください。

## 目次

- [出力フォーマットの変更](#出力フォーマットの変更) (破壊的変更)
- [追加した機能](#追加した機能)
- [修正したバグ](#修正したバグ)
- [開発とリリースの基盤](#開発とリリースの基盤)

## 出力フォーマットの変更

> [!WARNING]
> **破壊的変更**: export の出力フィールド名を elasticsearch-dump 互換に変更しました。
> 本家が出力した既存のダンプファイルは、この fork の import では読めません。
> `_id` と `_source` を探すため、`ID` と `RawData` しか持たない行からは
> どちらも取り出せません。
>
> これは**エラーにならず、静かに失敗します**。import は
> `finish import row count 2` のように件数を報告して終了コード 0 で終わりますが、
> ドキュメントは 1 件も入りません (本文が空、かつ ID も空になるため)。
> 実測で確認した挙動です。本家と行き来する場合は、事前にフォーマットを
> 変換してください。

本家の出力です。

```json
{"ID":"163820696","RawData":{"id":163820696,"asset":"","imageUrl":""}}
```

この fork の出力です。`_index` と `_score` が加わり、`ID` は `_id` に、
`RawData` は `_source` に変わりました。

```json
{"_index":"my_index","_id":"163820696","_score":1,"_source":{"id":163820696,"asset":"","imageUrl":""}}
```

> [!NOTE]
> `_score` は常に `1` の固定値です。実際のスコアではありません。scroll は
> スコアを計算しないため意味のある値を持たず、フィールドの形を揃えるために
> 置いています。

Elasticsearch のレスポンスのヒットと同じフィールド名にしたことで、
[elasticsearch-dump](https://github.com/elasticsearch-dump/elasticsearch-dump)
が出力する JSON Lines と同じ形になります。`jq` などの既存のツールを
そのまま使えるようにするための変更です。

## 追加した機能

> [!NOTE]
> 追加したフラグはすべて既定値が従来の挙動と同じです。
> 何も指定しなければ本家と同じ動作になります。

### sliced scroll による並行エクスポート (`--slices`, `--size`)

scroll は前のレスポンスが返す scroll ID がないと次のページを要求できず、
本質的に逐次です。往復の遅延がそのままスループットの上限になります。
`--slices` はインデックスを互いに素な N 個の部分集合に分け、それぞれを
独立した scroll として同時に読みます。

```shell script
./esdump export --index my_index -o - --slices 3 --size 1000
```

3 シャード、1 億9200 万件のインデックスから 100 万件を export した実測値です
(3 回の中央値)。

| slices | size | time | speedup |
| --- | --- | --- | --- |
| 1 | 100 | 245.0 s | 1.00 |
| 3 | 100 | 91.2 s | 2.69 |
| 1 | 1000 | 160.8 s | 1.52 |
| **3** | **1000** | **72.9 s** | **3.36** |
| 6 | 1000 | 243.9 s | 1.05 |

> [!IMPORTANT]
> **`--slices` はプライマリシャード数に合わせてください。**
> 各スライスは担当するシャードの全ドキュメントを走査するため、シャード数より
> 多いスライスを指定すると同じデータを何度も読むことになります。上の表では
> 3 シャードに対する 6 スライスが、分割しない場合とほぼ同じ速度まで落ちています。

既定値は `--slices 1` と `--size 100` で、本家と同じ挙動です。

### ログ出力間隔の指定 (`--progress-every`, `--pages-every`)

本家は 10,000 件ごとに進捗を 1 行出力します。2 億件のインデックスでは
20,000 行を超え、ログローテーションの世代が進捗行だけで埋まり、
エラーがその中に埋もれます。

```shell script
# 100,000 件ごとに 1 行にする
./esdump export --index my_index -o - --progress-every 100000

# 進捗行とフェッチ時間の行を完全に止める
./esdump export --index my_index -o - --progress-every 0 --pages-every 0
```

既定値は進捗が 10,000 件ごと、フェッチ時間が 1,000 ページごとで、本家と同じです。

### ログのタイムゾーン指定 (`--log-timezone`)

本家のログのタイムスタンプにはタイムゾーンの表記がありません。ローカルタイムで
出力されるため、`2026/09/07 12:42:55` という行を UTC と読み違えます。複数の
ホストのログを並べる場合や、別の環境でログを読む場合に取り違えます。

```shell script
./esdump export --index my_index -o - --log-timezone Asia/Tokyo
./esdump export --index my_index -o - --log-timezone UTC
```

解釈できないタイムゾーン名はエラーで終了します。既定の書式に落として続行すると、
長時間の export が意図しないタイムゾーンのログを残したまま完走してしまうためです。

## 修正したバグ

> [!WARNING]
> 以下のうち終了コードと出力の破損に関わるものは、**失敗を成功と誤認する**
> 類の不具合です。cron や CI から esdump を呼んでいる場合、本家では
> 取りこぼしに気づけません。

### export の失敗が終了コードに出ない

本家は cobra の `Run` を使っています。`Run` は戻り値を持たないため、
エラーが呼び出し側に伝わらず、終了コードは常に 0 でした。一部のスライスが
失敗して全件揃っていない場合でも、シェルや cron からは成功と区別できません。
`RunE` に変更し、`Execute()` がエラーを受けて exit 1 するようにしました。
import 側も同様です。

### scroll のエラーで出力ファイル全体が失われる

本家は取得中のエラーで `log.Fatalln` を呼びます。`os.Exit` は defer を
実行しないため、gzip writer の `Close` と `bufio.Writer` の `Flush` が
飛びます。それまでに取得したデータがバッファごと失われ、出力は途中で
切れた不完全な gzip ストリームになります。数時間かけた export の終盤で
1 度エラーが出ただけで、成果物全体が使えなくなっていました。
エラーは戻り値で返し、`errors.Join` で集約するようにしました。

> [!NOTE]
> 修正したのは scroll の取得中に起きるエラーです。**接続の確立そのものに
> 失敗する経路は本家のまま残っています。** `cmds/es.go` の `getEsClient` が
> クライアント生成の失敗で `panic` するため、到達できないエンドポイントを
> 指定すると exit 2 で異常終了し、defer が走らないため出力ファイルは
> 0 バイトのまま残ります。終了コードは非ゼロなので失敗の検知はできます。

### 書き込みの失敗を捨てて exit 0 で終わる

`Flush` と `Close` の戻り値を捨てていました。4MB の `bufio` バッファと
gzip の内部バッファに残ったデータは、最後の `Flush` と `Close` で初めて
ファイルへ届きます。ENOSPC や `-o -` でのパイプ切断がここで起きても、
書き出し中は成功していたため戻り値は nil のままでした。gzip のフッタ
(CRC32 と ISIZE) も欠けたファイルが、正常終了として残っていました。

### 非 gzip のファイルを import して 0 件で正常終了する

`ImportData` が gzip のヘッダエラーで、名前付き戻り値の `err` (この時点で
nil) を返していました。gzip ではないファイルを渡しても 0 件を import して
正常終了するため、`export | import` のパイプラインで壊れたダンプと成功を
区別できませんでした。

### scroll を読み切った `io.EOF` で異常終了する

`olivere/elastic` の `ScrollService.Do` は、ヒットが 0 件になった時点で
`io.EOF` を返します。総件数が `--size` の倍数ちょうどのとき、最終ページが
ページサイズと同数になるため、次の要求が `io.EOF` を返します。本家はこれを
エラーとして扱うため、全件を読み終えているのにプロセスが落ちて出力が
失われていました。

### 空インデックスへの sliced scroll が 400 で落ちる

一度もドキュメントを入れていないインデックスに `--slices 2` 以上を指定すると、
OpenSearch が `field _id not found` の 400 を返します。スライスの分割は既定で
`_id` のフィールドデータを使いますが、空のセグメントには `_id` が存在しない
ためです。0 件として正常終了するようにしました。

判定は `field _id not found` まで絞っています。`search_phase_execution_exception`
だけで判定すると、マッピングの不整合や不正なクエリまで 0 件として飲み込み、
取りこぼしに気づけなくなるためです。

### `--size` を変えると最終ページを検出できない

本家は終了判定をリテラルの `100` と突き合わせています。

```go
if len(res.Hits.Hits) < 100 {
```

ページサイズを変えられるようにする以上、この比較も `--size` の値に
追随させなければなりません。追随しないと、100 件未満のページが来るまで
読み続けるか、逆に途中で打ち切ることになります。`--size` の追加と
不可分の修正です。

### 書き出しに失敗するとプロセスがハングする

本家は書き込みエラーで受信ループを `break` します。受信側が消えるため、
取得側は `dataChan` への送信でブロックしたまま止まります。`wg.Wait()` が
返らず `close` も行われないため、プロセスが終わりません。チャネルの
バッファは 300 しかないため、大きなインデックスでは確実にこの状態に
入ります。エラー後は書き出しをやめ、チャネルだけを読み進めるようにしました。

### `-o -` で標準出力を閉じ、オープン失敗で panic する

本家は出力先を無条件に `defer ofile.Close()` します。`-o -` のときは
`os.Stdout` を閉じるため、以降のログ出力が失われます。標準出力の場合は
閉じないようにしました。

また、この `defer` はオープンの成否を確認する前に積まれています。
オープンに失敗すると `ofile` が nil のまま `Close()` が呼ばれ、nil ポインタ
参照で panic します。成否を先に確認するようにしました。

### サマリのログが実際より小さい gzip サイズを報告する

サイズを `Flush` と `Close` より前に読んでいました。バッファの内容は
そこで初めてファイルへ届くため、残っている分を数えていません。
30 万件で 2.44 MB と 2.63 MB の差が出ました。

### `storeTime` が常に 0 になる

出力側にかけた時間を測る変数に、測定値を代入していませんでした。ログの
`storeTime` は常に `0.000000` でした。取得待ち (`totalFetchTime`) と
突き合わせてボトルネックを判断するための値なので、測っていなければ
意味がありません。

### `json.Marshal` のエラーを捨てる

エラーを捨てていたため、失敗すると空の `bs` がそのまま書かれ、壊れた行が
出力に混ざったまま正常終了していました。

## 開発とリリースの基盤

コードの挙動ではありませんが、本家にはない仕組みです。

### 統合テスト

本家にテストはありません。sliced scroll の全件性とページ境界は、実際の
scroll API を叩かないと検証できないため、モックではなく OpenSearch の
コンテナを使います。

```shell script
make test/short        # ユニットテストのみ。コンテナ不要
make test/integration  # OpenSearch を起動して全テストを実行
make test/down         # コンテナを停止して削除
```

統合テストは `DeleteIndex` を実行します。復旧不能な操作であるため、接続先を
ループバックに限定し、インデックス名を `esdump_test_` 始まりに強制する
2 段のガードを入れています。詳細は [Readme.md の Tests](Readme.md#tests) を
参照してください。

### リリースの自動化

[tagpr](https://github.com/Songmu/tagpr) と
[GoReleaser](https://goreleaser.com/) でリリースを自動化しています。
本家の `.github/workflows/release.yml` は削除しました。
配布対象は `darwin/amd64`, `darwin/arm64`, `linux/amd64`, `linux/arm64` で、
本家が配布していた windows-amd64 は対象外です。

### サプライチェーン対策

GitHub Actions の SHA 固定、コンテナイメージのダイジェスト固定、Dependabot に
よる更新、`actionlint` / `zizmor` / `pinact` / `govulncheck` の CI 実行、
リリース資産への SBOM 添付を行っています。方針と理由は
[CLAUDE.md](CLAUDE.md#サプライチェーン対策) に記録しています。
